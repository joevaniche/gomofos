import React from 'react';
import StaticPage from '../components/StaticPage';

export default function Careers() {
  return (
    <StaticPage tag="WORK WITH US" title="Careers" testId="careers-page">
      <p>We're a small, opinionated team. If you've ever rage-quit, debated whether peeker's advantage is real, or shipped code at 2am — we'll get along.</p>
      <p>Open roles are listed here as they become available.</p>
    </StaticPage>
  );
}
